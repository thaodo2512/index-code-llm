# AI-assistant config & planning scratch — kept locally, never published
CLAUDE.md
AGENTS.md
.claude/
.cursor/
.codex/
.github/copilot-instructions.md
*.plan.md
docs/plan*.md

# Local runtime / machine-specific data
views/
.codegraph/
workspace.json

# Build output & dependencies
dist/
node_modules/
*.tsbuildinfo

# Logs & misc
*.log
.DS_Store
coverage/

# Generated deployments (machine-specific)
deploy/
.env

# clients/ holds PUBLISHED sample configs & skills — re-include despite the
# AI-config patterns above (those target our local files, not the templates)
!clients/**
